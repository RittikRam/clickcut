spring.application.name=url-shortener-sb

# Server configuration for Replit
server.port=8080
server.address=0.0.0.0

# Database configuration using Replit environment variables
spring.datasource.url=${DATABASE_URL}
spring.datasource.username=${PGUSER}
spring.datasource.password=${PGPASSWORD}
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# JWT configuration
jwt.secret=bb817f8b04429165323eaa4932783f4a54c523657c4914b56abe444d00965a93
jwt.expiration=172800000

# Logging configuration
logging.level.org.springframework.security=INFO
logging.level.org.url.shortener=INFO
logging.level.org.springframework.web=INFO